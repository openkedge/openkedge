name: Safe Deployment

on:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run OpenKedge
        uses: ./
        with:
          intent: examples/intents/describe-instances.json
